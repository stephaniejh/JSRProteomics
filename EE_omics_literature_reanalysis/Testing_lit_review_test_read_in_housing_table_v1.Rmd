---
title: "Testing_lit_review_test_read_in_housing_table_v1"
author: "Stephanie Huang"
date: "2025-12-19"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

### Load Packages

```{r}
library(readxl)
library(dplyr)
library(stringr)
library(ggplot2)
library(gt)
library(lubridate)
```

# Read in data

```{r}
rat_df <- read_excel("input/test_2025_12_27_EE_omics_lit_review_mRNA_Proteins.xlsx",
                              sheet = "rat_housing")
```


```{r}
str(rat_df)
summary(rat_df)
colnames(rat_df)
```


```{r}
housing_rat_calc_1 <- housing_mice_df %>% 
  group(animal_type, year, author, housing) %>%
  ()
```

```{r}
m
```


## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
